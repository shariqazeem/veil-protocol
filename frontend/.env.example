# ─── Required for relayer ────────────────────────────────────
RELAYER_ACCOUNT_ADDRESS=0x...
RELAYER_PRIVATE_KEY=0x...

# ─── RPC (use a dedicated provider for mainnet) ─────────────
# STARKNET_RPC_URL=https://starknet-mainnet.g.alchemy.com/starknet/version/rpc/v0_7/YOUR_KEY

# ─── Garaga calldata server ─────────────────────────────────
# CALLDATA_SERVER_URL=http://localhost:3001

# ─── x402 Micropayments ─────────────────────────────────────
# Treasury address that receives x402 micropayment fees
# Defaults to the deployer address from addresses.json
# X402_TREASURY_ADDRESS=0x...

# Relay flat fee: defaults to $0.03 USDC / 0.015 STRK
# RELAY_FEE_USDC=0.03
# RELAY_FEE_STRK=0.015

# Premium analysis fee: defaults to $0.01 USDC / 0.005 STRK
# PREMIUM_PRICE_USDC=0.01
# PREMIUM_PRICE_STRK=0.005

# Set to "false" to disable x402 relay path (legacy 2% only)
# X402_RELAY_ENABLED=true

# ─── Relayer fee (legacy path without x402) ──────────────────
# Basis points deducted from withdrawal (200 = 2%)
# RELAYER_FEE_BPS=200

# Use V1 (ETH gas) transactions instead of V3 (STRK gas)
# RELAYER_ETH_GAS=false

# ─── Telegram Bot (optional) ────────────────────────────────
# BOT_TOKEN=...
# NOTIFY_WEBHOOK_URL=...
# NEXT_PUBLIC_RELAYER_URL=/api/relayer
